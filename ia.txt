1: src/data/mascotasa.js, Importa el array en App.jsx y verifica que carga en console.log
2: fue útil ya que importó con éxito el array de las mascotas.js
3: no se modificó nada

-------------------------
1.: src/components/MascotaCard.jsx.
- El componente recibe por props: nombre, raza, edad, especie, descripcion, características.
- Muestra la información de fomra visual y ordendada.
- Aplica un estilo diferenciado por especie (color/etiqueta distinto para Perro, Gato, Otro).

También, define los tipos de las PropTypes y entrega valores por defecto para evitar errores cuando falte un dato. Renderiza el texto siempre con contenido JSX normal. No uses dangerouslySetInnerHTML. No dejes credenciales, claves ni datos sensibles en el código
2: muy útil ya que agregó estilos y coloresd distintos para diferenciar
3: se modificó App.tsx para importar bien el componente MascotaCard

-------------------------
1:src\components\ListaMascotas.jsx

-Recorre el array con .map() y renderiza un MascotaCard por cada mascota
-Asigna correctamente la prop key usando el id de cada elemento (no uses el índice del array)
-En App.jsx, renderiza ListaMascotas pasandole los datos importados
2: bastante útil, creó sin errorres la lista de las mascotas asignandole la prop key usando el id de cada usando el id de cada uno 
3: no se tuvo que modificar nada en esta etapa
-------------------------
1: src\components\FiltroEspecie.jsx

-con botones o un selector para filtrar por la especie: "Todas", "Perro", "Gato", "Otro".
-En App.jsx, usa UseStates para guardar el filtro seleccionado y un campo de búsqueda por nombre.
-Aplica ambos filtros sobre el array antes de pasarlo a ListaMascotas.
-Usa renderizado condicional para mostrar el mensaje "No hay mascotas que coincidan" cuando el resultado quede vacío.

Ademas, el campo de busqueda debe ser un componente controlado (su valor vive en el eestado de React).
Valida y normaliza el texto de busqueda (recorta espacios y limita el largo) antes de usarlo, para no confiar ciegamente en la entrada del usuario.
2: la busqueda y filtro por especie tuvo éxito y funciona con todas las especificaciones dadas
3: En esta estapa no hubo modificaciones
-------------------------
